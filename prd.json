{
  "project": "3ds-qwen-remake",
  "goal": "Enhance the shipped Ashen Rift 3DS game: feel, density, variety, audio, polish \u2014 without ever breaking the build or the 30fps floor.",
  "notes": "Phase-gated. Phase 0 = calibration. Phase 1 = modularization (enriched 2026-08-16 from a live survey; protect lists grepped from current files). Phase 2 = enhancement seeds - enrich with exact code sites after phase 1 lands. Extraction stories are TWO-STAGE: new TU first, trimmed original second; the duplicate-symbol link error between stages is expected and drives stage 2.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Calibration: build probe constant wired through main",
      "phase": "0",
      "priority": 1,
      "passes": false,
      "description": "Create include/demake/build_probe.hpp declaring exactly: `namespace demake { constexpr int kRalphLoopGeneration = 1; }` with a header guard. In source/main.cpp, add `#include \"demake/build_probe.hpp\"` and a `static_assert(demake::kRalphLoopGeneration >= 1, \"ralph probe\");` at file scope. Touch nothing else. main.cpp is 20 lines; re-emit it completely.",
      "acceptanceCriteria": [
        "build_probe.hpp exists with the constant",
        "main.cpp includes and static_asserts it",
        "full verify chain green"
      ],
      "verify": "tools/ralph-verify.sh quick && grep -q 'kRalphLoopGeneration' include/demake/build_probe.hpp && grep -q 'demake/build_probe.hpp' source/main.cpp && grep -q 'static_assert(demake::kRalphLoopGeneration' source/main.cpp",
      "allowedFiles": [
        "include/demake/build_probe.hpp",
        "source/main.cpp"
      ],
      "protect": {}
    },
    {
      "id": "US-010",
      "title": "Extract creature/actor rendering into renderer_actors.cpp",
      "phase": "1",
      "priority": 10,
      "passes": false,
      "description": "renderer.cpp is 1476 lines. Move these Renderer member function DEFINITIONS, unchanged, into a new source/renderer_actors.cpp: renderHumanoid, renderHorse, renderDeer, renderFox, renderCrane, drawBlobShadow, renderBoar, renderMountainOgre, renderWarden (currently lines 613-1100). They are member functions: moving them to another .cpp needs NO header change \u2014 do not touch include/demake/renderer.hpp. If a moved function uses a file-local helper or constant from renderer.cpp (e.g. kCube, anonymous-namespace helpers), declare it in a new include/demake/renderer_internal.hpp and keep one definition. STAGE 1 (this iteration): emit ONLY source/renderer_actors.cpp and, if needed, include/demake/renderer_internal.hpp. Do NOT emit renderer.cpp yet \u2014 the duplicate-symbol link error is expected and is the signal for stage 2. STAGE 2 (next iteration, when LAST_VERIFY shows duplicate symbols): emit ONLY the trimmed source/renderer.cpp with those nine definitions removed and nothing else changed.",
      "acceptanceCriteria": [
        "nine actor render functions live in renderer_actors.cpp",
        "renderer.cpp under 1000 lines",
        "zero behavior change, full verify green"
      ],
      "verify": "tools/ralph-verify.sh && test -f source/renderer_actors.cpp && grep -q 'void Renderer::renderWarden' source/renderer_actors.cpp && grep -q 'void Renderer::renderHorse' source/renderer_actors.cpp && [ $(grep -c 'void Renderer::renderWarden' source/renderer.cpp) -eq 0 ] && [ $(wc -l < source/renderer.cpp) -lt 1000 ] && [ $(wc -l < source/renderer_actors.cpp) -lt 560 ]",
      "allowedFiles": [
        "source/renderer_actors.cpp",
        "source/renderer.cpp",
        "include/demake/renderer_internal.hpp"
      ],
      "protect": {
        "source/renderer.cpp": [
          "bool Renderer::initialize()",
          "void Renderer::render(",
          "void Renderer::updateCamera(",
          "void Renderer::renderUi(",
          "void Renderer::shutdown()"
        ]
      }
    },
    {
      "id": "US-011",
      "title": "Extract HUD/UI rendering into renderer_hud.cpp",
      "phase": "1",
      "priority": 11,
      "passes": false,
      "description": "After US-010, move Renderer::renderUi and Renderer::drawText definitions (plus any file-local helpers ONLY they use: distanceToSegment, objectiveFor, loadTextAsset) from source/renderer.cpp into a new source/renderer_hud.cpp. Member functions: NO header change, do not touch include/demake/renderer.hpp. Shared file-local helpers go through include/demake/renderer_internal.hpp. STAGE 1: emit ONLY renderer_hud.cpp (+ renderer_internal.hpp if needed). STAGE 2 (after the duplicate-symbol link failure): emit ONLY the trimmed renderer.cpp.",
      "acceptanceCriteria": [
        "renderUi and drawText live in renderer_hud.cpp",
        "renderer.cpp under 700 lines",
        "full verify green"
      ],
      "verify": "tools/ralph-verify.sh && test -f source/renderer_hud.cpp && grep -q 'void Renderer::renderUi(' source/renderer_hud.cpp && [ $(grep -c 'void Renderer::renderUi(' source/renderer.cpp) -eq 0 ] && [ $(wc -l < source/renderer.cpp) -lt 700 ] && [ $(wc -l < source/renderer_hud.cpp) -lt 480 ]",
      "allowedFiles": [
        "source/renderer_hud.cpp",
        "source/renderer.cpp",
        "include/demake/renderer_internal.hpp"
      ],
      "protect": {
        "source/renderer.cpp": [
          "bool Renderer::initialize()",
          "void Renderer::render(",
          "void Renderer::updateCamera(",
          "void Renderer::shutdown()"
        ]
      }
    },
    {
      "id": "US-012",
      "title": "Extract combat controllers into combat_controllers.cpp",
      "phase": "1",
      "priority": 12,
      "passes": false,
      "description": "core.cpp is 904 lines. Move the definitions of PlayerController::update, PlayerController::finishTimedState, PlayerController::damage, BossController::update, BossController::damage (currently lines 548-862) unchanged into a new source/combat_controllers.cpp. Member functions: NO change to include/demake/core.hpp. File-local helpers they use (clamp, facingTo, playerCanAct, forwardOf, offset, distance, normalized, circlesOverlap, isBossZone, bossMaximumHealth, zoneGroundHeight...) stay defined once \u2014 expose the needed ones through a new include/demake/core_internal.hpp. STAGE 1: emit ONLY combat_controllers.cpp + core_internal.hpp. STAGE 2 (after duplicate-symbol link failure): emit ONLY the trimmed core.cpp.",
      "acceptanceCriteria": [
        "five controller methods live in combat_controllers.cpp",
        "core.cpp under 700 lines",
        "host tests untouched and green"
      ],
      "verify": "tools/ralph-verify.sh && test -f source/combat_controllers.cpp && grep -q 'void PlayerController::update(' source/combat_controllers.cpp && grep -q 'void BossController::update(' source/combat_controllers.cpp && [ $(grep -c 'void BossController::update(' source/core.cpp) -eq 0 ] && [ $(wc -l < source/core.cpp) -lt 700 ] && [ $(wc -l < source/combat_controllers.cpp) -lt 420 ]",
      "allowedFiles": [
        "source/combat_controllers.cpp",
        "source/core.cpp",
        "include/demake/core_internal.hpp"
      ],
      "protect": {
        "source/core.cpp": [
          "void ZoneManager::update(",
          "void GameSimulation::step(",
          "void GameSession::step(",
          "void ZoneManager::enter(",
          "GameSimulation::GameSimulation()"
        ]
      }
    },
    {
      "id": "US-020",
      "title": "Hit-pause on melee connect",
      "phase": "2",
      "priority": 20,
      "passes": false,
      "description": "ENRICH-BEFORE-RUN after phase 1 lands: a 3-tick hit-stop when a player attack connects (named constant kHitPauseTicks), applied in the fixed-step update, excluded from camera update so the camera stays smooth. Manager names the exact combat-resolution function first.",
      "acceptanceCriteria": [
        "kHitPauseTicks constant exists and is used",
        "verify chain green"
      ],
      "verify": "tools/ralph-verify.sh && grep -rq 'kHitPauseTicks' include/demake source",
      "allowedFiles": [],
      "protect": {}
    },
    {
      "id": "US-021",
      "title": "Boss phase-transition audio stingers",
      "phase": "2",
      "priority": 21,
      "passes": false,
      "description": "ENRICH-BEFORE-RUN: one-shot stinger cue when a boss crosses its phase threshold, through the existing AudioStreamer double-buffer path (no new allocation per frame). Manager stages the stinger PCM into assets first and names the phase-threshold code site.",
      "acceptanceCriteria": [
        "stinger triggers on phase change",
        "verify chain green"
      ],
      "verify": "tools/ralph-verify.sh",
      "allowedFiles": [],
      "protect": {}
    },
    {
      "id": "US-022",
      "title": "Sunlit Reach ambient density pass within zone budget",
      "phase": "2",
      "priority": 22,
      "passes": false,
      "description": "ENRICH-BEFORE-RUN: raise prop/wildlife density in assets/zones/field.json (grass tufts, stones, birds) while validate-assets budget stays green. Values from BRIEF.md palette; no new asset types, only more instances of existing IDs.",
      "acceptanceCriteria": [
        "field.json prop count rises",
        "budget report green",
        "verify chain green"
      ],
      "verify": "tools/ralph-verify.sh",
      "allowedFiles": [
        "assets/zones/field.json"
      ],
      "protect": {}
    }
  ]
}